package com.github.blackjak34.compute.enums;

/**
 * A very, very simple enum that only serves to give a name
 * to the various states of the emulator present in
 * {@link TileEntityComputer}.
 * 
 * @author Sam
 * @since 1.0
 */
public enum StateComputer {
	RESET, RUN, HALT;
}
